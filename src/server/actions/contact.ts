"use server"

export const contactFunc = async (formdata: any):Promise<any> => {
    console.log(formdata);
    // Here you can write contact form logic. The form data is stored in the formdata variable
}